<!doctype html>
<html lang="en">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="minimum-scale=1, initial-scale=1, width=device-width" />
    <title>{{ metadata.title }}</title>
    <meta name="description" content="{{ metadata.description }}">
    <!-- <link rel="stylesheet" href="./styles/custom.css"> -->
    <link rel="stylesheet" href="/style.css?v={% version %}"/>

</head>

<body class="container mx-auto px-8 pt-16">

    <nav class="pb-16">
        {% set navPages = collections.all | eleventyNavigation %}
        <ul class="flex">
            {%- for entry in navPages %}
            <li {% if entry.url==page.url %} class="flex-1 mr-2 mr-6 uppercase underline" {% endif %} class="flex-1 mr-2 mr-6">
                <a href="{{ entry.url | url }}" class="text-blue-500 hover:text-blue-800">{{ entry.title }}</a>
            </li>
            {%- endfor %}
        </ul>
    </nav>
    <main class="prose prose-lg max-w-full">
        <h1>{{ title }}</h1>
        <article>{{ content | safe }}</article>
        
    </main>
   
    <footer class="pt-4 mt-8 border-t-2 border-black">
        <p>All contnet BY-NC-CC {{ metadata.title }}</p>
    </footer>
</body>

</html>