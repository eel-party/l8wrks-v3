---
layout: layout
pagination:
  data: collections.item
  size: 10
  reverse: true
  alias: items
---

<!-- <p><small style="color: red;">this is using the secnd-lvl template</small></p>
 -->
{{ content | safe }}

{% for item in items | reverse %}
  <!-- compapare the item's parentID to the book's key -->
  {% if item.data.parent == eleventyNavigation.key %}
    <article>
      <h2>
        <a href="{{ item.url | url }}">{{ item.data.title }}</a>
      </h2>
      <img src="/assets/{{ item.data.image }}" alt="{{ item.data.description }}" class="img" width="200"/>

      <!-- <p>{{ content | safe }}</p> -->
    </article>
  {% endif %}
{% endfor %}